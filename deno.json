{
  "name": "@chronal/core",
  "version": "0.0.7",
  "description": "A tiny and fast date utility library for modern JavaScript runtimes.",
  "license": "MIT",
  "exports": "./src/mod.ts",
  "tasks": {
    "test": "deno test src/**/*.test.ts* --allow-read --allow-net --allow-env --unstable",
    "publish": "deno run --allow-read --allow-write --allow-run scripts/publish.ts",
    "check": "deno fmt src/**/*.ts* && deno lint && deno check \"src/**/*.ts*\""
  },
  "imports": {
    "@deno/dnt": "jsr:@deno/dnt@0.42.3",
    "@std/assert": "jsr:@std/assert@1"
  }
}
